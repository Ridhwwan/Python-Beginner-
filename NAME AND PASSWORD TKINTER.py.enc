from tkinter import *

root = Tk()


name = Label(root, text="Name")
password = Labe(root, text="Password")
namee = Entry(root)
passwordd = Entry(root)

name = Entry(row=0)
password = Entry(row=0)

namee.grid(row=0, column=1)
passwordd.grid(row=1, column=1)

myname = ('')

mypassword = ('')

    if myname == 'Salaudeen Ridhwan':
    mypassword == 'mycode'

   print("WELCOME")

    elif:
        myname != Salaudeen Ridhwan
    elif:
        mypassword != mycode
    print("SORRY, INCORRECT USER INPUT")

root.mainloop() 

